#pragma once
#ifndef LOAD_READS_H
#define LOAD_READS_H

// load_reads.h

#include <vector>
#include <string>

void load_reads(std::vector<std::string>& Read, std::string read_left_file, std::string read_right_file);  // load all the data from file

#endif 